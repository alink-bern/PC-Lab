# PC Lab

A Projekt about building computers for kids and adolescents (Schnupperlernende).
The project was AI generated and was not optimized for other uses or scaling.

# RUN

To setup the project clone it from the repository.

run the code locally
`npm run dev`

build the project
`npm run build`
